pipeline{
    agent any

    stages{
        stage('init'){
            steps{
                sh 'terraform init -no-color'
            }
        }
        stage('Validate'){
            steps{
                sh 'terraform validate -no-color'
            }
        }
    }
}